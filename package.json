{
    "name": "el-aire-norteno",
    "private": true,
    "scripts": {
        "prestart": "next build",
        "start": "next start -p ${PORT}"
      },
    "dependencies": {
        "cron": "^1.7.1"
    }
}
